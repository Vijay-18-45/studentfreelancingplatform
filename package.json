{
  "name": "campus-gig-plain",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "echo 'No linting needed for plain JS version'"
  },
  "dependencies": {
    "vite": "^6.2.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
